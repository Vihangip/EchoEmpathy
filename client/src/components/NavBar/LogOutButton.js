import {Button} from "@mui/material";
import {styled} from "@mui/system";

export const LogOutButton = styled(Button) ({
    fontSize: '1rem',
    width: '10rem',
    height: '3rem',
    background: 'black',
    borderRadius: '1.5rem',
    color: 'white',
    margin: '1rem',
    marginLeft: "auto"
})