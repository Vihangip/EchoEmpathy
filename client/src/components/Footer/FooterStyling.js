import {styled} from "@mui/system";
import {Typography} from "@mui/material";

export const CustomFooter = styled(Typography)({
    color: 'white',
    marginTop: 'auto',
    backgroundColor: 'black',
    height: '3rem',
    justifyItems: 'center',
    paddingTop: '1rem',
    textJustify: 'center'
});